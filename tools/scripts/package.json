{
  "name": "@cpa/tools-scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "tsx --env-file-if-exists=../../.env --test \"*.test.ts\"",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "onboard-tenant": "tsx --env-file-if-exists=../../.env onboard-tenant.ts",
    "reconcile-prompt-suggestion-prs": "tsx --env-file-if-exists=../../.env reconcile-prompt-suggestion-prs.ts",
    "smoke:data-plane": "tsx --env-file-if-exists=../../.env smoke-data-plane.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@cpa/agents": "workspace:*",
    "@cpa/auth": "workspace:*",
    "@cpa/db": "workspace:*",
    "@cpa/integrations": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
